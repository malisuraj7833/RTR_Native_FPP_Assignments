If we call resize() in initiaise() then the scene displayed(Triangle) get cut on the top and sometimes and right corner

To avoid cutting of sucg edges we will transalte the vector deep into the screen by moving its -z axis. 

Here we are doing model transformation by moving object.